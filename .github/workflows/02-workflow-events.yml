name: 02 - Workflow Events
on:
  workflow_dispatch:
  # pull_request:
#   schedule:
#     - cron: "*/5 * * * *"

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the Trigger
        run: echo ${{ github.event_name }}
